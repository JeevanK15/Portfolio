import { NextResponse } from "next/server";
import { achievements } from "@/data/achievements"; import { certifications } from "@/data/certifications"; import { education } from "@/data/education"; import { experiences } from "@/data/experience"; import { leadership } from "@/data/leadership"; import { profile } from "@/data/profile"; import { projects } from "@/data/projects"; import { skillGroups } from "@/data/skills";
const context = JSON.stringify({ profile, education, experiences, projects, skillGroups, certifications, achievements, leadership });
export async function POST(request: Request) { try { const body = await request.json() as { messages?: { role: "user" | "assistant"; content: string }[] }; const messages = body.messages?.slice(-10) ?? []; const latest = messages.at(-1)?.content?.trim() ?? ""; if (!latest || latest.length > 600) return NextResponse.json({ error: "Please ask a shorter question." }, { status: 400 }); const key = process.env.AI_API_KEY; const url = process.env.AI_API_URL ?? "https://api.groq.com/openai/v1/chat/completions"; const model = process.env.AI_MODEL ?? "llama-3.1-8b-instant"; if (!key) return NextResponse.json({ error: "The assistant is currently unavailable." }, { status: 503 }); const response = await fetch(url, { method: "POST", headers: { Authorization: `Bearer ${key}`, "Content-Type": "application/json" }, body: JSON.stringify({ model, temperature: .2, max_tokens: 300, messages: [{ role: "system", content: `You are the official AI assistant for Jeevan K's portfolio. Answer only with verified information in this context. If unavailable, say you don't have that information. Never invent projects, companies, technologies, achievements, dates, links, or personal information. Do not claim unlisted professional experience. Keep answers concise and natural. Context: ${context}` }, ...messages] }), signal: AbortSignal.timeout(15000) }); if (!response.ok) return NextResponse.json({ error: "The assistant could not respond." }, { status: 502 }); const data = await response.json() as { choices?: { message?: { content?: string } }[] }; return NextResponse.json({ answer: data.choices?.[0]?.message?.content?.trim() || "I don't have that information in Jeevan's portfolio context." }); } catch { return NextResponse.json({ error: "The assistant is temporarily unavailable." }, { status: 500 }); } }
