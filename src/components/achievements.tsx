import { achievements } from "@/data/achievements";
export function Achievements() { return <section id="achievements" className="section-wrap"><div className="section-head"><span className="section-index">07</span><div><h2>Milestones that mark the work.</h2><p>Achievements / earned recognition</p><div className="hairline mt-6" /></div></div><div className="timeline">{achievements.map((achievement, index) => <article className="timeline-item glass" key={achievement.title}><span className="technical-label">0{index + 1} · {achievement.subtitle}</span><h3>{achievement.title}</h3><p>{achievement.description}</p></article>)}</div></section>; }
