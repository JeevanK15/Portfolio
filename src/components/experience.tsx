import { CalendarDays, MapPin } from "lucide-react";
import { experiences } from "@/data/experience";
export function Experience() { return <section id="experience" className="section-wrap"><div className="section-head"><span className="section-index">03</span><div><h2>Learning by stepping into the work.</h2><p>Experience / field notes</p><div className="hairline mt-6" /></div></div><div className="timeline">{experiences.map((experience, index) => <article key={experience.company} className="timeline-item glass"><span className="technical-label">0{index + 1} · Internship</span><h3>{experience.role}</h3><p className="text-foreground">{experience.company}</p><div className="mt-4 flex flex-wrap gap-4 text-xs text-muted"><span className="inline-flex items-center gap-2"><CalendarDays size={13} />{experience.startDate} — {experience.endDate}</span><span className="inline-flex items-center gap-2"><MapPin size={13} />{experience.location}</span></div><ul className="mt-6 grid gap-3 text-sm leading-7 text-muted">{experience.description.map((item) => <li key={item}>+ {item}</li>)}</ul><div className="mt-6 flex flex-wrap gap-2">{experience.technologies.map((technology) => <span className="border border-border px-2 py-1 text-xs text-muted" key={technology}>{technology}</span>)}</div></article>)}</div></section>; }
