"use client";
import Link from "next/link";
import { Check, Code2, Copy, Link2, Mail, MoveUpRight } from "lucide-react";
import { useState } from "react";
import { profile } from "@/data/profile";
const footerLinks = [{ label: "About", href: "#about" }, { label: "Skills", href: "#skills" }, { label: "Experience", href: "#experience" }, { label: "Projects", href: "#projects" }, { label: "Contact", href: "#contact" }];
export function Footer() { const [copied, setCopied] = useState(false); async function copyEmail() { await navigator.clipboard.writeText(profile.email); setCopied(true); window.setTimeout(() => setCopied(false), 1800); } return <footer className="site-footer"><div className="footer-word" aria-hidden="true">JEEVAN K</div><div className="section-wrap relative"><p className="technical-label">Open to meaningful work</p><h2 className="mt-5 max-w-4xl text-5xl font-semibold leading-[.88] tracking-[-.08em] sm:text-8xl">LET&apos;S BUILD<br /><span className="text-accent">SOMETHING MEANINGFUL.</span></h2><p className="mt-7 max-w-xl leading-7 text-muted">Building today, learning for tomorrow, and creating thoughtful digital experiences along the way.</p><a href={`mailto:${profile.email}`} className="cta-button mt-8">Start a conversation <MoveUpRight size={15} /></a><div className="mt-20 grid gap-10 border-t border-border pt-8 sm:grid-cols-3"><div><p className="font-semibold">{profile.name}</p><p className="mt-2 text-sm text-muted">{profile.tagline}</p></div><nav className="grid gap-3 text-sm" aria-label="Footer navigation">{footerLinks.map((link) => <Link className="footer-link" href={link.href} key={link.label}>{link.label} <MoveUpRight size={13} /></Link>)}</nav><div className="grid gap-3 text-sm"><a className="footer-link" href={profile.githubUrl}><Code2 size={15} /> GitHub</a><a className="footer-link" href={profile.linkedinUrl}><Link2 size={15} /> LinkedIn</a><a className="footer-link" href={`mailto:${profile.email}`}><Mail size={15} /> Email</a><button className="footer-link w-fit" type="button" onClick={copyEmail}>{copied ? <Check size={15} /> : <Copy size={15} />} {copied ? "Copied" : "Copy email"}</button></div></div><p className="mt-12 border-t border-border pt-5 text-xs text-muted">© 2026 {profile.name}. Built with curiosity.</p></div></footer>; }
