import { ArrowUpRight, Code2, ExternalLink } from "lucide-react";
import { projects } from "@/data/projects";
export function Projects() { return <section id="projects" className="section-wrap"><div className="section-head"><span className="section-index">04</span><div><h2>Small products, seriously considered.</h2><p>Projects / built to learn by doing</p><div className="hairline mt-6" /></div></div>{projects.map((project, index) => <article key={project.id} className="project-showcase"><div className="project-visual"><div className="project-ui">{project.id === "pycheck" ? <><span className="technical-label">PYTHON / CHECK</span><div className="code-lines">{["def validate(source):", "  errors = parse(source)", "  return correct(errors)", "# keep the logic intact"].map((line, i) => <i key={line} style={{ "--line": `${40 + i * 12}%` } as React.CSSProperties} />)}</div></> : <><span className="technical-label">EXPENSE / INSIGHT</span><div className="bill-chart">{[40, 68, 52, 88, 62].map((height) => <i key={height} style={{ "--height": `${height}%` } as React.CSSProperties} />)}</div><p className="mt-4 text-xs text-muted">OCR · NLP · CATEGORY</p></>}</div><span className="absolute bottom-5 left-5 technical-label">PRODUCT PREVIEW / 0{index + 1}</span></div><div><span className="technical-label">Project 0{index + 1} · {project.status}</span><h3 className="project-title">{project.name}</h3><p className="text-xl text-accent">{project.tagline}</p><p className="project-copy mt-6 max-w-xl">{project.description}</p><div className="project-meta"><div><span className="technical-label">Problem</span><p>{project.problem}</p></div><div><span className="technical-label">Approach</span><p>{project.solution}</p></div></div><div className="mt-7 flex flex-wrap gap-2">{project.technologies.map((tech) => <span key={tech} className="border border-border px-2 py-1 text-xs text-muted">{tech}</span>)}</div><div className="mt-8 flex flex-wrap gap-4">{project.liveUrl ? <a href={project.liveUrl} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 text-sm text-foreground hover:text-accent"><ExternalLink size={15} /> View demo <ArrowUpRight size={14} /></a> : <span className="inline-flex items-center gap-2 border border-border px-3 py-2 text-sm text-muted" aria-disabled="true" title="Add the live demo URL in src/data/projects.ts">Demo link pending</span>}{project.githubUrl ? <a href={project.githubUrl} target="_blank" rel="noreferrer" className="inline-flex items-center gap-2 text-sm text-foreground hover:text-accent"><Code2 size={15} /> Repository <ArrowUpRight size={14} /></a> : <span className="text-sm text-muted">Private repository</span>}</div></div></article>)}</section>; }
